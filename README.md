# 📘 Projectile Motion Simulation

A simple and interactive simulation of **projectile motion** built with **HTML, CSS, and JavaScript**.  
Users can add multiple projectiles, customize launch parameters, and visualize their trajectories in real time.

---

## 🚀 Features

- Add multiple projectiles with:
  - Initial velocity (v₀)
  - Launch angle (θ)
  - Initial height (h₀)
- Real-time animation with:
  - Position, velocity, tangential acceleration (aₜ), normal acceleration (aₙ)
  - Horizontal distance traveled (range)
- Zoom & pan freely on the canvas
- Pause/Resume & Reset simulation
- Automatically generated projectile list

---

## 📂 Project Structure
/
├── index.html # Main UI
├── style.css # Styling
├── script.js # Physics + rendering + interaction logic
└── README.md

## 🎮 How to Use
### **1. Requirements**
- Đã cài đặt **Git**  
  Kiểm tra bằng:
  ```bash
  git --version
### **2. Clone repository**
If you're using Terminal, run this code:
```git clone https://github.com/BAROKHUU/Projectile-Motion-Simulation```
If you're using SSH, run this code:
```git clone git@github.com:BAROKHUU/Projectile-Motion-Simulation.git ```

Then go inside the project directory:
``` cd Projectile-Motion-Simulation ```